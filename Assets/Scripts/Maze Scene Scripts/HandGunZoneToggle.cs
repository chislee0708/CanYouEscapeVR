using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HandGunZoneToggle : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // onTriggerEnter(Collider colidedObj)
    // {
    //     // if(GameObject.Find)
    // }

    // Update is called once per frame
    void Update()
    {
        
    }
}
